const GUARDIAN_LINK_STATUSES = Object.freeze([
  'PENDING_VERIFICATION', 'LINKED', 'DECLINED', 'EXPIRED', 'REVOKED',
]);
const GUARDIAN_LINK_EVENTS = Object.freeze([
  'INVITED', 'RESENT', 'ACCEPTED', 'DECLINED', 'EXPIRED', 'REVOKED',
]);
const GUARDIAN_LINK_ACTORS = Object.freeze(['LEARNER', 'GUARDIAN_LINK_TOKEN', 'SYSTEM']);
const GUARDIAN_LINK_TTL_MS = 72 * 60 * 60 * 1000;
const GUARDIAN_REFERENCE_PATTERN = /^CY-GL-[0-9A-HJKMNP-TV-Z]{20}$/;

module.exports = {
  GUARDIAN_LINK_ACTORS,
  GUARDIAN_LINK_EVENTS,
  GUARDIAN_LINK_STATUSES,
  GUARDIAN_LINK_TTL_MS,
  GUARDIAN_REFERENCE_PATTERN,
};
